List transactions.
---
parameters:
  - in: query
    name: customer_id
    type: integer
    required: false
    description: The customer's identifier
  - in: query
    name: date
    type: string
    required: false
    description: The transaction's date
  - in: query
    name: page
    type: integer
    required: false
    decription: The list transaction's value for pagination
responses:
  200:
    description: success
